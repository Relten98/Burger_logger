<li>
	{{name}}

	<button class="change-devoured primary-btn m-1" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Eat!{{else}}REORDER{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">X<i class="fas fa-times"></i></button>
</li>